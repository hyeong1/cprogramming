// 단일 열결 리스트 구현
#include <stdio.h>

typedef struct_node Node;
struct_node {
    int data;
    Node *next;
}

typedef struct _list {
    Node *head;
    Node *tail;
    int size;
} List;

int main() {

    return 0;
}