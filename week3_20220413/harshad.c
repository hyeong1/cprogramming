#include <stdio.h>

int main()
{
    // 하샤드 수 -> x의 자릿수의 합으로 x가 나누어짐
    // 자연수 x를 입력받아 하샤드 수인지 아닌지 검사하는 프로그램
    int x;
    printf("자연수를 입력하세요(단, 1이상, 10000이하인 정수): ");
    scanf("%d", &x);

    // x는 다섯자리수
    if (x == 10000){

    }
    // x는 
    else if (x < 10000){

    }
    else if (x < 999){

    }
    else if (x < 99){

    }
    else{

    }

    return 0;
}